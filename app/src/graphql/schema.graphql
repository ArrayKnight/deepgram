type Query {
  todos: [Todo!]
}

type Todo {
  id: String!
  title: String!
  description: String!
  status: Boolean!
}

type Mutation {
  addTodo(todoInput: TodoInput!): Todo!
}

input TodoInput {
  title: String!
  description: String!
}
